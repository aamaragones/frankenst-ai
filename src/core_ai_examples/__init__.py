"""Repository reference package built on top of `frankstate`.

`core_ai_examples` contains the concrete layouts, models, prompt assets,
configuration files and utilities used to demonstrate how the reusable
`frankstate` contracts are applied in a real LangGraph project.

It is intentionally treated as the reference package of this mono-repo, not as
the stable public API of the published base `frankstate` wheel.
"""
